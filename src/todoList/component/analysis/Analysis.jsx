import React from 'react';import Tabs from './../common/Tabs.jsx';import Toolbar from './../common/ToolBar.jsx'import './analysis.less'class Analysis extends React.Component {    constructor(props) {        super(props);        this.state = {            cur: 0,            percent: 50        }        this.tabs = [            {label: '泵排量', value: 'analysis.png'},            {label: '循环压耗', value: 'recycle.png'},            {label: '钻头压降', value: 'clearify.png'},            {label: '液流变参数', value: 'clearify.png'},            {label: '环空返速', value: 'back-speed.png'},            {label: '流态', value: 'back-speed.png'},        ]    }    getPercent() {        return 32*this.state.percent*0.01;    }    setValue(v) {        if(v >= 100) {            v = 100;        }        if(v <= 0) {            v = 0;        }        this.setState({            percent: v        })    }    render() {        return (<div className="ana-content">            <div className="shiubeng-bg">               <img className="water-tower" src={`./image/analysis/animate-${this.state.cur+1}.png`} />               <div className="water-bg" style={{height: `${this.getPercent()}%`}}>                   <img className="water" src={`./image/analysis/water.png`}/>               </div>            </div>            <div className="analysis-tabs">                <Tabs dataSource={this.tabs} withInput={true} getValue={this.setValue.bind(this)}/>            </div>            <Toolbar />            </div>)    }    componentDidMount() {        let i = 0;        setInterval(() => {            if(i === 6) {                i = 0;            }            this.setState({                cur: i            })            i++        }, 1000)    }}export default Analysis;